// HOOFSTUK 1
// Je hebt meerdere values:

// Booleans: True en false
// Numbers: 1  2  3  4 etc.
// Strings: "alleswathiertussenvalt"
// Undefined:
// Null: Gebruik je als iets later nog gebruikt zal worden, 
// zo kan een ander zien dat er nog wat met bijvoorbeeld de functie gaat gebeuren.

// Operators zijn de tekens waar je mee rekent:

// +: 2+2=4
// -: 2-2=0
// *: 2*4=8
// /: 4/2=2
// %: Deelbaar door ... maakt ...

// == : Gelijk aan.
// != : Niet gelijk aan.
// === : Gelijk aan en bestaand uit dezelfde type.
// !== : Geeft true door als de type hetzelfde is maar de rest niet.
// && betekend "EN", || betekend "of"


// HOOFDSTUK 2
// let: Is een variabele die alleen te vinden is binnen de ScopedCredential.
// const: Is een variabele die ook buiten de scope te vinden zal zijn.
// do & while: Elke keer als while true doorgeeft gaat de do statement in.
// for: Kun je een loop mee MSMediaKeyNeededEvent.


// HOOFDSTUK 3
// Each binding has a scope, which is the part of the program in which the binding is visible.
// let en const zijn twee verschillende bindings die alleen binnen hun blok te gebruiken en te zien zijn.
// Var is global te zien en door de gehele functie heen.
// Het scopen door je bestand heen noemen we "lexical scoping".

// Een functie die zichzelf called noemen we "recursive".
// Looping is sneller dan recursive gebruik.

// A pure function is a specific kind of value-producing function that not only has no side effects but also 
// doesn’t rely on side effects from other code—for example, it doesn’t read global bindings whose value might change.


// HOOFDSTUK 4
// Properties that contain functions are generally called methods of the value they belong to.
// The push method adds values to the end of an array, and the pop method does the opposite, removing the last value in the array and returning it.
// 
// A stack, in programming, is a data structure that allows you to push values into it and pop them out again in the opposite order so that the thing 
// that was added last is removed first.

// Een delete operator kan een string afbreken.