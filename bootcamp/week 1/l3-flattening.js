// THE FLATTENING
// Maak meerdere arrays 1 array

// reduce zorgt ervoor dat de array 1 waarde mee geeft.
// concat voegt argumenten samen en returnt een nieuwe string.


//     --------------------------------------------------------------------------------

let arrays = [
  [1, 2, 3],
  [4, 5],
  [6]
];

console.log(arrays.reduce(acc, curValue)) => acc.concat(curValue), []));